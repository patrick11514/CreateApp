{
    "name": "%%NAME%%",
    "version": "0.0.1",
    "main": "build/index.js",
    "type": "module",
    "scripts": {
        "dev": "tsx watch ./src/index.ts",
        "build": "rm -rf build && mkdir -p build && tsc && tsc-alias",
        "start": "node ./build/index.js",
        "check": "tsc --noEmit"
    },
    "dependencies": {},
    "devDependencies": {}
}
